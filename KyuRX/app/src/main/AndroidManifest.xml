<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.selsoft.kyurx">

    <dist:module dist:instant="true" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.prescription_details.PrescriptionDetails"></activity>
        <activity android:name=".ui.bit_status.BitStatus" />
        <activity android:name=".ui.medicine_details.MedicineDetails" />
        <activity android:name=".ui.nearby_pharmacy.NearByPharmacy" />
        <activity
            android:name=".ui.main.PatientMain"
            android:label="@string/title_activity_patient_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ui.splash.Welcome"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.login.PatientRegister"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ui.add_prescription.AddPrescription" />
        <activity
            android:name=".ui.login.RegisterActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ui.main.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>